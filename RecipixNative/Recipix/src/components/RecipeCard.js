import React from 'react';
import { View, Text } from 'react-native';

const RecipeCard = props => {
  console.log('props', props);
  return (
    <View>
      <Text>This is recipe card</Text>
    </View>
  );
};

export default RecipeCard;
